// Code generated by goctl. DO NOT EDIT.
// goctl 1.10.1
// Source: oss.proto

package logic

import (
	"bytes"
	"context"
	"time"

	"oss/internal/dao"
	"oss/internal/svc"
	"oss/oss"

	"lukechampine.com/blake3"

	"github.com/zeromicro/go-zero/core/logx"
)

type UploadPartLogic struct {
	ctx    context.Context
	svcCtx *svc.ServiceContext
	logx.Logger
}

func NewUploadPartLogic(ctx context.Context, svcCtx *svc.ServiceContext) *UploadPartLogic {
	return &UploadPartLogic{
		ctx:    ctx,
		svcCtx: svcCtx,
		Logger: logx.WithContext(ctx),
	}
}

// 上传分片（流式）并流式更新 BLAKE3 哈希
func (l *UploadPartLogic) UploadPart(stream oss.OSS_UploadPartServer) error {
	var uploadID string
	var objectKey string
	var partNumber int32
	var allData []byte

	for {
		req, err := stream.Recv()
		if err != nil {
			if err.Error() == "EOF" {
				break
			}
			l.Errorf("stream recv error: %v", err)
			return err
		}

		// 第一次接收时记录 uploadID 和 partNumber
		if uploadID == "" {
			uploadID = req.UploadId
			partNumber = req.PartNumber

			// 从缓存获取 objectKey
			meta, err := l.svcCtx.RedisDao.GetUploadMeta(l.ctx, uploadID)
			if err != nil || meta == nil {
				l.Errorf("GetUploadMeta error: %v", err)
				return err
			}
			objectKey = meta.ObjectKey
		}

		// 累积分片数据
		data := req.Data
		if len(data) > 0 {
			allData = append(allData, data...)
		}
	}

	if len(allData) == 0 {
		return nil
	}

	// 流式更新 BLAKE3 哈希
	if h, ok := l.svcCtx.Hashers.Load(uploadID); ok {
		h.(*blake3.Hasher).Write(allData)
	}

	etag, err := l.svcCtx.Storage.UploadPart(
		l.ctx,
		uploadID,
		int(partNumber),
		objectKey,
		bytes.NewReader(allData),
		int64(len(allData)),
	)
	if err != nil {
		l.Errorf("UploadPart error: %v", err)
		return err
	}

	// 缓存分片信息
	partMeta := &dao.PartMeta{
		PartNumber: partNumber,
		ETag:       etag,
		Size:       int64(len(allData)),
		UploadedAt: time.Now().Unix(),
	}
	if err := l.svcCtx.RedisDao.AddPart(l.ctx, uploadID, partMeta); err != nil {
		l.Errorf("AddPart cache error: %v", err)
	}

	// 返回响应
	if err := stream.SendAndClose(&oss.UploadPartResp{
		Etag:       etag,
		PartNumber: partNumber,
	}); err != nil {
		l.Errorf("SendAndClose error: %v", err)
		return err
	}

	return nil
}
